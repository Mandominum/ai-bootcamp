{
  "buildCommand": "cd todo-app && npm install && npm run build",
  "outputDirectory": "todo-app/dist",
  "installCommand": "cd todo-app && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
